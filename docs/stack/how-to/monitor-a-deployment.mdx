---
title: Monitor a deployment
description: >-
  Expose Lineth health and metrics endpoints, and watch finalization, data
  availability, and consensus signals
image: /img/socialCards/monitor-a-deployment.jpg
---

import GlossaryTerm from "@theme/GlossaryTerm";

This page describes how to expose health and metrics endpoints on a
<GlossaryTerm term="Lineth" /> deployment, and what to watch once they are scraped.
<GlossaryTerm term="Operator">Operators</GlossaryTerm> define alert thresholds and recovery
objectives for their own environment.

## Prerequisites

- A running Lineth deployment, or the [local evaluation stack](./run-local-lineth-stack.mdx)
- [Prometheus](https://prometheus.io/docs/introduction/overview/), or another
  metrics backend that can scrape Prometheus text format

## Expose health and metrics

Some deployment components can expose `/health` and `/metrics` HTTP endpoints.
The `/health` endpoint tells you whether the process answers liveness checks.
The `/metrics` endpoint exports named counters and gauges in
[Prometheus text format](https://prometheus.io/docs/instrumenting/exposition_formats/).
For each component you operate, do the following:

1. Set the option that opens the HTTP server.
2. Allow Prometheus (or another scraper) to reach that host and port.
3. Poll `http://<host>:<port><endpoint>` with HTTP `GET` on an interval, or add
   that URL as a Prometheus scrape target.
4. Filter series by the component metrics prefix.

The following table lists the options, default port, paths, and metrics prefix for
each component.

<table className="table-fit-cols">
  <thead>
    <tr>
      <th>Component</th>
      <th>Option(s)</th>
      <th>Default port</th>
      <th>Health endpoint</th>
      <th>Metrics endpoint</th>
      <th>Metrics prefix</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <GlossaryTerm term="Coordinator" />
      </td>
      <td>
        <a href="../../reference/component-configuration/linea-coordinator-options#api">
          <code>api.observability-port</code>
        </a>
      </td>
      <td>
        <code>9545</code>
      </td>
      <td>
        <code>/health</code>
      </td>
      <td>
        <code>/metrics</code>
      </td>
      <td>
        <code>linea_</code>
      </td>
    </tr>
    <tr>
      <td>
        <GlossaryTerm term="Maru" />
      </td>
      <td>
        <a href="../../reference/component-configuration/linea-maru-options#observability">
          <code>observability.port</code>
        </a>
      </td>
      <td>
        <code>9545</code>
      </td>
      <td>
        <code>/health</code>
      </td>
      <td>
        <code>/metrics</code>
      </td>
      <td>
        <code>maru_</code>
      </td>
    </tr>
    <tr>
      <td>
        <GlossaryTerm term="Prover" />
      </td>
      <td>
        <a href="../../reference/component-configuration/linea-prover-options#controller">
          <code>controller.prometheus.enabled</code>
        </a>
        <br />
        <a href="../../reference/component-configuration/linea-prover-options#controller">
          <code>controller.prometheus.port</code>
        </a>
        <br />
        <a href="../../reference/component-configuration/linea-prover-options#controller">
          <code>controller.prometheus.route</code>
        </a>
      </td>
      <td>
        <code>9090</code>
      </td>
      <td>Unsupported</td>
      <td>
        <code>/metrics</code>
      </td>
      <td>
        <code>prover_</code>
      </td>
    </tr>
    <tr>
      <td>
        <GlossaryTerm term="Postman" />
      </td>
      <td>
        <a href="../../reference/component-configuration/linea-postman-options#api">
          <code>API_PORT</code>
        </a>
      </td>
      <td><code>3000</code></td>
      <td>Unsupported</td>
      <td>
        <code>/metrics</code>
      </td>
      <td>
        <code>linea_postman_</code>
      </td>
    </tr>
  </tbody>
</table>

:::note Prover options
Set the prover [`controller.prometheus.enabled`](../../reference/component-configuration/linea-prover-options#controller)
option to `true` before you scrape it.
The prover metrics endpoint is [`controller.prometheus.route`](../../reference/component-configuration/linea-prover-options#controller)
if you change it from the default `/metrics`.
:::

The coordinator also polls the
<GlossaryTerm term="Finalization layer">finalization layer</GlossaryTerm>
to track finalized state.
Set the poll interval, retries, and warning thresholds in
[`l1-finalization-monitor`](../../reference/component-configuration/linea-coordinator-options#l1-finalization-monitor).

## Alert on these signals

Use these signals to see whether the deployment is producing blocks, posting data, and finalizing.
Create alert rules in Prometheus or your other metrics backend for the signals you operate.
Trigger a notification when a signal stops advancing or crosses a threshold you set based on your
deployment's block time, finalization interval, and capacity.

- **Service health:** Poll the `/health` endpoint on the coordinator and Maru to confirm each process is up and serving.
  The prover and Postman don't have a `/health` endpoint; poll their `/metrics` endpoint to confirm those
  processes are reachable.
- **Data availability posting:** Watch the `linea_blob_*` metrics to see whether the coordinator is
  submitting blobs or other data availability payloads.
  See [Data availability and finalization](../deployment/data-availability-finalization)
  for blob posting success and gas.
- **Finalization:** Watch the `linea_aggregation_*` metrics on the coordinator to see whether proofs are settling.
  Confirm [`currentL2BlockNumber`](/api/linea-smart-contracts/zkevmv2#currentl2blocknumber)
  advances after a successful
  [`finalizeBlocks`](/api/linea-smart-contracts/linearollup#finalizeblocks) on the rollup contract,
  which is when the finalized head on the Lineth network moves.
- **Gas price:** Watch the `linea_gas_price_cap_*` metrics on the coordinator to see cost pressure on
  finalization layer submissions.
- **Consensus latency:** Watch the Maru block-building window in a multi-validator deployment to
  confirm validators still have time to produce a block.
  See [Monitor Maru](./set-up-distributed-sequencing#monitor-maru) for the key metrics and
  recommended threshold.
- **Storage capacity:** Check proving artifacts and databases to confirm they still have room to grow.
  See [High availability](../deployment/high-availability#within-region-redundancy).

## Next steps

- Review example failure behavior in
  [High availability](../deployment/high-availability.mdx).
- Restore Maru quorum using
  [Set up distributed sequencing](./set-up-distributed-sequencing.mdx#restore-lost-quorum).
